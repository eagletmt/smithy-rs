[package]
name = "chaos"
version = "0.1.0"
authors = ["Russell Cohen <russell.r.cohen@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
aws-hyper = { path = "../../aws-hyper" }
dynamodb = { path = "../../dynamodb", package = "aws-sdk-dynamodb" }
http = "0.2.4"
smithy-http = { path = "../../smithy-http"}

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
